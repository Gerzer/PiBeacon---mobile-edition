project 'PiBeacon Client - mobile edition.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PiBeacon Client - mobile edition' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PiBeacon Client - mobile edition
  pod 'Alamofire', '~> 4.5'

  target 'PiBeacon Client - mobile editionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PiBeacon Client - mobile editionUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
